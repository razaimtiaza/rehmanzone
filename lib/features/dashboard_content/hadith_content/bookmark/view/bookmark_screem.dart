import 'package:flutter/material.dart';

class BookMarkHadith extends StatelessWidget {
  const BookMarkHadith({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
